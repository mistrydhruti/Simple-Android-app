<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dhrutiarunmistry.details">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Placement cell"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup" />
        <activity android:name=".Start" />
        <activity android:name=".Aboutus" />
        <activity android:name=".Admin" />
        <activity android:name=".Cgpa" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

        <activity android:name=".cards" />
        <activity android:name=".asianpaints" />
        <activity android:name=".avalon" />
        <activity android:name=".bluestar" />
        <activity android:name=".capitalfirst" />
        <activity android:name=".citi" />
        <activity android:name=".indiabulls" />
        <activity android:name=".Login" />
        <activity android:name=".password" />
        <activity android:name=".Profile" />
        <activity android:name=".Update_password" />
        <activity android:name=".Update_profile" />
        <activity android:name=".Registration" />
        <activity
            android:name=".SecondActivity"
            android:label="@string/title_activity_second" />
    </application>

</manifest>